<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="hr_applicant_view_form_extended" model="ir.ui.view">
            <field name="name">hr_applicant_view_form_extended</field>
            <field name="model">hr.applicant</field>
            <field name="inherit_id" ref="hr_recruitment.hr_applicant_view_form"/>
            <field name="type">form</field>

            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <button name="generate_invite_token" string="Invite Applicant To Portal " type="object" class='oe_highlight' attrs="{'invisible':['|', ('portal_uid','!=', False), ('invite_token','!=', False)]}" />
                </xpath>
                <xpath expr="//field[@name='emp_id']" position="after">
                    <field name="invite_url" attrs="{'invisible':['|', ('portal_uid','!=', False), ('invite_token','=', False)]}" />
                    <field name="invite_token" invisible="1" />
                    <field name="portal_uid" invisible="1" />
                </xpath>
            </field>
        </record>
<!--        email template for sending the invite url for portals-->
        <record id="portal_invite_email_template" model="mail.template">
            <field name="name">Edari Portal Invite e-mail template</field>
            <field name="model_id" ref="edari_timesheet.model_hr_employee"/>
            <field name="email_from">no-reply@edarione.com</field>
            <field name="subject">Edari portal invite url</field>
            <field name="email_to">${object.work_email or object.private_email|safe}</field>
            <field name="lang">${object.lang}</field>

            <field name="auto_delete" eval="True"/>
            <field name="body_html">
                <![CDATA[
	              <p>Dear ${(object.name)},<br/><br/>
	              Please use the below url for signing up for the edari portal!<br/></p>
                  Regards,<br/>
                  <p>${(object.invite_url)}</p>
                  <p>${(object.invite_url)}</p>
                  ${(object.company_id.name)}
	            ]]>
            </field>
        </record>
    </data>
</odoo>